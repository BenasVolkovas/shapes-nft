import React from "react";

const Install = () => {
    return (
        <div>
            <h3>Folow the link to install</h3>
            <a href="https://metamask.io/download/">Meta Mask</a>
        </div>
    );
};

export default Install;
